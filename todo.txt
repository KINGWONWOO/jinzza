JINZZA (진짜를 찾아라) - Setup TODO
Based on docs/gamestarter.docx, section 15 "제작 타임라인" Week 1, cross-checked against
the actual project state on 2026-08-26.

=================================================================
DONE ALREADY (before this session)
=================================================================
- UE 5.8 project created (C++), First Person template as base
  (jinzzaCharacter / jinzzaGameMode / jinzzaPlayerController / jinzzaCameraManager)
- Git repo initialized with a UE-appropriate .gitignore/.gitattributes

=================================================================
DONE THIS SESSION (by Claude)
=================================================================
- Enabled AudioCapture + Synthesis plugins in jinzza.uproject
  (needed later for the real-time voice pitch-shift system, section 8-2/11)
- Added [OnlineSubsystem]/[OnlineSubsystemSteam] config block to
  Config/DefaultEngine.ini, using Valve's public test App ID (480) as a
  placeholder so Steam sessions work locally before you have a real App ID
- Reorganized all Content into /Game/JINZZA/ via the live editor's asset
  tools (proper engine-side move, not a filesystem move, so references and
  the level's World Partition actors stayed intact):
  FirstPerson, Variant_Horror, Variant_Shooter, Characters, Weapons,
  LevelPrototyping and Input all now live under Content/JINZZA/.
  EditorStartupMap/GameDefaultMap/GlobalDefaultGameMode in
  Config/DefaultEngine.ini were repointed to match.
  Left Collections/ and Developers/ where they were (editor-managed, not
  real content).

=================================================================
DONE THIS SESSION (2026-08-30, by Claude) - pulls Week 11 forward early
=================================================================
- Main menu: Host Game now creates the Steam session immediately with
  default FJinzzaMatchSettings and travels straight to Lvl_Lobby - no
  pre-create Host Setup screen. Removed the Join Game button and the whole
  room-list browse/search flow (UjinzzaJoinWidget/RoomRow, GameInstance's
  RefreshSessionList/FindSessions/JoinSessionAtIndex) - joining is invite-
  only now via the existing Steam overlay invite accept path.
- Added a physical "Room Settings" kiosk (AjinzzaRoomSettingsKiosk) placed
  in Lvl_Lobby: walk up, press E, and a panel opens showing the former
  Host Setup fields (room name/max players/judge+vote count/phase speed/
  role assign). Host-only editable (same HasAuthority()+local-controller
  idiom UjinzzaLobbyWidget's Start Match button already used); writes
  straight into AjinzzaLobbyGameState::MatchSettings, replicating live to
  everyone already in the lobby. Proximity is polled on a 0.2s timer from
  AjinzzaLobbyPlayerController (the lobby's ADefaultPawn has no collision
  set up for overlap events) rather than driven by overlaps; "Interact" is
  a classic Action Mapping (E) added to DefaultInput.ini alongside Enhanced
  Input, which coexist fine.
- Built a full Settings screen (Graphics/Audio/Controls/Gameplay tabs),
  replacing the old placeholder, backed by a new UjinzzaGameUserSettings
  (registered via GameUserSettingsClassName in DefaultEngine.ini):
    - Graphics: window mode/resolution/vsync/frame cap + the 9 individual
      scalability categories, all real UGameUserSettings base-class calls.
    - Audio: Master/Music/SFX/Voice sliders. No SoundClass/SoundMix assets
      exist in Content yet, so these are created as runtime-transient
      USoundClass/USoundMix objects (real engine API, same runtime pattern
      lots of shipped indie UE games use) - Master is fully audible today;
      Music/SFX/Voice will govern any future audio content assigned to
      those child classes.
    - Controls: mouse sensitivity + invert-Y (wired into AjinzzaCharacter::
      DoAim), and key rebinding for the button-press Input Actions (Jump/
      Shoot/SwapWeapon/Sprint) via per-player overrides applied onto a
      DuplicateObject'd runtime copy of each Input Mapping Context
      (AjinzzaPlayerController::BuildRuntimeMappingContext) - the shared
      .uasset IMCs are never mutated.
    - Gameplay: subtitles toggle (flag only, no subtitle content yet),
      colorblind mode + strength via the engine's real accessibility API
      (UWidgetBlueprintLibrary::SetColorVisionDeficiencyType).
  All of this mirrors the settings architecture Epic's own Lyra sample
  project uses (UGameUserSettings + Enhanced Input mapping overrides), per
  the user's ask to build on a real reference rather than invent one.
- New JinzzaUI namespace (Source/jinzza/UI/jinzzaUIStyle.h/.cpp): shared
  rounded-brush button/panel/heading styling (dark noir palette, gold/
  crimson accents fitting the judge/vote theme) applied across the main
  menu, lobby, room settings, settings, and game-end widgets, plus a short
  fade-in on the main menu's button page. No texture/font assets used -
  still fully hand-built in C++ per this project's no-UMG-designer
  constraint (see UjinzzaMainMenuWidget's header comment).
- NOT done: moving the actual gameplay (Week 9's map blockout etc.) - this
  session only touched menu/lobby/settings plumbing per the user's request.
- Fixed a real gap in the Steam session setup, found by cross-checking
  against the user's own prior multiplayer project (github.com/KINGWONWOO/
  Unreal_Game_Noob): our session creation (CreateSession/JoinSession) was
  correct, but nothing told Unreal to actually route game traffic through
  Steam's P2P relay - ServerTravel/ClientTravel were still using the
  default raw-IP NetDriver, which only works for players who happen to be
  reachable by direct IP (same LAN, or manually port-forwarded). Added,
  matching that reference project's config exactly:
    - Config/DefaultEngine.ini: [/Script/Engine.GameEngine] NetDriver
      override to /Script/SteamSockets.SteamSocketsNetDriver (falls back
      to IpNetDriver if unavailable; scoped to GameEngine only, so PIE
      multiplayer testing in the editor is untouched), plus the
      OnlineSubsystemSteam P2P-relay settings (bAllowP2PPacketRelay,
      P2PConnectionTimeout/CleanupTimeout, bInitServerOnClient, VAC,
      GameServerQueryPort) and bHasVoiceEnabled=true under [OnlineSubsystem]
      (ahead of the planned voice/proximity chat feature, section 8-2).
    - jinzza.uproject: enabled the SteamSockets plugin (a separate plugin
      from OnlineSubsystemSteam, not enabled by default - the NetDriver
      override does nothing without it). [ ] Needs an editor RESTART to
      take effect - Live Coding can't hot-load a newly enabled plugin.
    - Added steam_appid.txt (480, matching SteamDevAppId) at the project
      root, same as the reference project - lets the Steamworks SDK
      initialize when launched from the editor/PIE instead of via Steam
      itself.
- Placed the RoomSettingsKiosk actor in Lvl_Lobby (world origin, where all
  12 PlayerStarts already face inward) via the live editor's scene tools,
  and saved the level. Verified via PIE that the main menu boots cleanly
  with the new styling and no runtime errors. Could not click-test the
  Host Game -> Lobby -> kiosk -> Settings flow end-to-end since no
  input-injection tool is exposed to this session - do a manual playtest
  pass before considering this feature done.

=================================================================
DONE THIS SESSION (2026-09-01, by Claude) - closes remaining gaps from
docs/steam_session_and_settings_reference.md's adaptation plan (section 7)
=================================================================
- Live player-count/session-data update (adaptation plan item 2): the Room
  Settings kiosk's Apply used to only write AjinzzaLobbyGameState::
  MatchSettings (replicated to players already in the lobby) without
  touching the actual advertised Steam session - so a widened player cap
  never reached Steam/friends looking at the invite. Added
  UjinzzaGameInstance::UpdateLiveSessionSettings(), called from
  UjinzzaRoomSettingsWidget::OnApplyClicked, which pushes NumPublicConnections
  and the searchable match-settings keys to the live session via
  IOnlineSession::UpdateSession (no destroy/recreate needed - Advanced
  Sessions' plugin isn't used here, but the native OSS interface has the
  same capability). Registered/cleared OnUpdateSessionComplete alongside
  the create/join/destroy delegates already there.
- Rich Presence (design doc section 8-1, adaptation plan item 5): added
  UjinzzaGameInstance::SetRichPresenceStatus(), called with "In Lobby" after
  a successful CreateSession or JoinSession. Confirmed OnlineSubsystemSteam
  actually implements IOnlinePresence (FOnlinePresenceSteam::SetPresence -
  checked the engine source directly) by calling ISteamFriends::
  SetRichPresence under the hood, so this needed no extra plugin (the
  reference doc's "Advanced Steam Sessions has a Set Rich Presence node"
  pointer was for their Blueprint-only stack; the native C++ IOnlinePresence
  interface does the same thing). Only "In Lobby" is published for now since
  URoundPhaseSubsystem (Week 4) doesn't exist yet to supply phase names -
  SetRichPresenceStatus is written to take a plain FString so wiring in
  per-phase text later is a one-line call from wherever phases change.
- Push-to-talk vs. open-mic + mic device selection (adaptation plan item 6):
  added EJinzzaMicInputMode + MicDeviceId to UjinzzaGameUserSettings (config-
  persisted, like the rest of that class) and a "Microphone" section to the
  Settings screen's Audio tab - a Push to Talk/Open Mic dropdown plus a
  device dropdown populated from UAudioCaptureBlueprintLibrary::
  GetAvailableAudioInputDevices (added "AudioCapture" to the module's
  PrivateDependencyModuleNames for this). Nothing consumes these yet since
  UVoiceDisguiseComponent/UProximityVoiceComponent don't exist (Week 6,
  still blocked on the Vivox/EOS decision below) - this just makes sure the
  preference exists and is stored before that component needs to read it,
  per the reference doc's explicit call-out that near-field voice needs
  this more than a typical project would.
- Verified (didn't need to change): typos already avoided, Find/Join-session
  was already a deliberate no-op (invite-only by design, matches "optional
  for a friends-only party game"), the [Voice] bEnabled=true baseline-VoIP
  trap was already avoided (bHasVoiceEnabled under [OnlineSubsystem] is a
  different, harmless flag - not the same section the reference project
  warned about), and Steam config/plugins were already C++-first with no
  Blueprint session logic to convert.
- [ ] Could not compile-check from this session (no exposed build-tool
  invocation) - the changes above touch GameInstance, GameUserSettings, two
  UI widgets, and the .Build.cs; do a build before relying on this. Fix Up
  Redirectors below still needed too.

=================================================================
DONE (by the user, 2026-09-01)
=================================================================
- [x] Fixed up redirectors in Content/JINZZA (the in-editor step this file
  used to list as outstanding).
- [x] Decided on the voice stack: Vivox. (Plugin still needs to be obtained
  and installed - see THINGS ONLY YOU CAN DO below; the decision itself was
  the blocker for UProximityVoiceComponent's design, not yet unblocked for
  implementation.)
- [x] Set up the remote: https://github.com/KINGWONWOO/jinzza (origin is
  already configured locally).
- [x] Confirmed solo development. Per the doc's own recommendation for a
  solo team, this session leaned on trimming: 2-judge mode and "host picks"
  role assignment are left as documented fallbacks-to-default rather than
  fully built (see Week 5 below) instead of building out their undecided
  rules now.

=================================================================
DONE THIS SESSION (2026-09-01, round 2, by Claude) - Week 4 + Week 5
=================================================================
Built the round-phase state machine and role/disguise assignment - the
first slice of actual match gameplay (everything before this was menu/
lobby/settings plumbing). New files:
- jinzzaRoundTypes.h: EJinzzaRoundPhase (the 8 phases + RoundComplete),
  EJinzzaPartyRole (RealOne/Imitator/Judge), EJinzzaFaceType (A/B/C),
  EJinzzaVoiceFilter (High/Low/Robot) - shared enums used across the new
  classes below.
- jinzzaGameGameState.h/.cpp (AGameStateBase, now set as Lvl_Game's
  GameStateClass): replicates CurrentPhase (RepNotify -> OnPhaseChanged
  delegate, mirroring the AFruitGameState pattern the reference doc called
  out) and PhaseEndServerTime (a single timestamp against
  GetServerWorldTimeSeconds(), not a ticking countdown - the standard
  low-bandwidth technique) plus MidEvaluationsRemaining.
- jinzzaRoundPhaseSubsystem.h/.cpp (UGameInstanceSubsystem, matches the
  design doc's class list section 11 exactly): server-only phase driver.
  StartRound()/AdvancePhase() walk the 8 phases with an FTimerHandle,
  writing each transition into AjinzzaGameGameState. Two things aren't
  fully spec'd in the doc and needed a judgment call, both commented in the
  code:
    - Mid-evaluation looping for VoteCount > 1 (design doc section 17 lists
      this as still-undecided): implemented as repeating MidEvaluation ->
      FreeTime2 as a pair VoteCount times before Interview. VoteCount=1
      collapses to exactly the doc's worked 6-player timing example.
    - PhaseSpeed (Slow/Normal/Fast) multipliers aren't numbered in the doc;
      used 1.3x/1x/0.75x, applied only to FreeTime1/FreeTime2/Interview per
      the doc's own description of what that preset scales ("자유시간·면담
      시간 일괄 비율 조정"). Tune from playtesting per section 17.
  Question-time cycle count is hardcoded to the doc's default of 2 (130s)
  - not wired to a lobby setting yet, there isn't one for it in
  FJinzzaMatchSettings.
- jinzzaPartyPlayerState.h/.cpp (APlayerState, set as both Lvl_Lobby's and
  Lvl_Game's PlayerStateClass): holds bIsGhost/FaceType/VoiceFilter as
  openly-replicated properties (they're visible/audible in-game anyway) but
  deliberately does NOT replicate the player's Role - that's the doc's core
  "정보 비대칭이 곧 서스펜스다" principle (the Judge must not be able to
  infer anything from replicated data). Role only ever exists server-side;
  see below for how each client learns their own role.
- jinzzaDisguiseComponent.h/.cpp (UActorComponent, added to AjinzzaCharacter
  so every character subclass gets one): applies FaceType to the
  third-person mesh (the one other players see - GetMesh() is already
  SetOwnerNoSee'd) via a dynamic material instance driving a "FaceIndex"
  scalar param. No MI_Face_A/B/C content exists yet (design doc section
  13-2), so this is a no-op until that material parameter exists - same
  "functional once content exists, harmless until then" pattern
  UjinzzaGameUserSettings already established for audio.
- AjinzzaGameGameMode (jinzzaGameGameMode.h/.cpp): now sets GameStateClass/
  PlayerStateClass to the new classes above, overrides StartPlay() to kick
  off UjinzzaRoundPhaseSubsystem::StartRound(), and implements AssignRoles()
  (Fisher-Yates shuffle of GameState->PlayerArray -> 1 Real One + 1 Judge +
  the rest Imitators; Imitators clone the Real One's FaceType/VoiceFilter).
  Also fixed DefaultPawnClass, which was still the bare ADefaultPawn from
  initial project generation (nobody had a mesh, so disguise had nothing to
  apply to) - now loads BP_FirstPersonCharacter. This is a real behavior
  change for anyone testing Lvl_Game: players now spawn as the actual
  character instead of a flying camera.
- AjinzzaGamePlayerController: added Client_ReceiveRoleAssignment (a Client
  Reliable RPC) + GetLocalRole()/GetKnownRealOne() - this is how role
  knowledge actually reaches each player: AssignRoles() sends the Real One
  "you're the Real One", each Imitator "you're an Imitator, here's who the
  Real One is" (an APlayerState* reference), and the Judge "you're the
  Judge" with nothing else - matching the reference doc's suggested shape
  (client RPCs for private per-player notifications) instead of trying to
  make replication itself conditionally hide data.
- AjinzzaLobbyGameMode: now also sets PlayerStateClass to
  AjinzzaPartyPlayerState, so seamless travel into Lvl_Game never has to
  worry about the carried-over PlayerState still being the base APlayerState.
- Deliberately NOT built (flagged as simplifications, not oversights):
  - 2-judge mode: AssignRoles() always assigns exactly 1 judge regardless
    of FJinzzaMatchSettings::JudgeCount. The design doc itself marks 2-judge
    as experimental/stretch with vote/interview rules undecided (sections 9
    and 15) - matches this session's solo-dev trim-scope conversation.
  - RoleAssignMethod == "Host Picks": falls back to Random. No picker UI
    exists for the host to choose who's the Real One before the round
    starts; building that UI wasn't in this round's scope.
  - Lobby-customization snapshot cloning (design doc's "역할 배정 시 위장
    복제" - the fuller version of what AssignRoles() does for face/voice):
    UCharacterCustomizationComponent (class list section 11) doesn't exist
    - there's no lobby appearance system yet for a Real One's look to be
    captured from. AssignRoles() only clones FaceType/VoiceFilter.
  - Voice filter isn't consumed anywhere yet - UVoiceDisguiseComponent/
    UProximityVoiceComponent (Week 6) don't exist. VoiceFilter is stored on
    AjinzzaPartyPlayerState and ready to be read once they do.
- [ ] Could not compile-check (still no exposed build-tool invocation this
  session either) or playtest (no input-injection tool exposed to drive PIE
  through an actual match) - do both before relying on this. In particular
  verify AjinzzaGameGameMode::StartPlay() actually runs after every
  seamlessly-traveled player's pawn/controller exists, since AssignRoles()
  depends on that.

=================================================================
ONE THING TO DO IN-EDITOR (5 minutes)
=================================================================
(Nothing outstanding - the redirector fixup above is done.)

=================================================================
THINGS ONLY YOU CAN DO (accounts, external tools, purchases)
=================================================================
[ ] Register a Steamworks partner account + create the App ID for this game
    (steamworks partner site) - replace SteamDevAppId=480 in
    Config/DefaultEngine.ini with the real one once issued.
[ ] Get access to Vivox (decision made - see DONE above) and install its
    plugin into the engine, then re-enable/configure it in jinzza.uproject:
    Vivox is NOT bundled with this UE 5.8 install (checked Engine/Plugins/
    Online - no Vivox plugin present). Get the standalone Vivox Unreal
    SDK/plugin directly from Vivox (or, if that path proves difficult,
    Epic Online Services' built-in Vivox-backed EOS Voice Chat is the
    fallback - needs an EOS Product/Client on the Epic Dev Portal instead).
    This unblocks UVoiceDisguiseComponent/UProximityVoiceComponent (Week 6,
    class list section 11).

=================================================================
UPCOMING BUILD WORK (can be done with Claude Code, later sessions -
listed in doc order, not urgent right now)
=================================================================
Week 2 - Movement & networking:
[ ] First/third-person movement+camera polish, listen-server replication test
    (partially covered as a side effect of Week 5's DefaultPawnClass fix -
    still needs an actual polish/test pass)

Week 3 - Lobby skeleton:
[ ] USteamLobbySubsystem, variable player-count slots, WBP_Lobby draft
    (functionally covered already by UjinzzaGameInstance + AjinzzaLobbyGameState
    + AjinzzaRoomSettingsKiosk from the first session, just under different
    class names than the doc's - not revisited this round)

Week 4 - Round state machine: DONE this round (see above).

Week 5 - Roles & disguise: DONE this round (see above), except 2-judge mode
and lobby-customization-snapshot cloning (deliberately deferred, see above).

Week 6 - Voice system:
[ ] UVoiceDisguiseComponent (pitch shift), UProximityVoiceComponent (depends
    on the Vivox/EOS decision above)

Week 7 - Question time & voting:
[ ] UQuestionTimeSubsystem (simultaneous submit + reveal), AVoteManager,
    WBP_VoteUI

Week 8 - 1:1 interview & ghost state:
[ ] Interview-target assignment, isolated voice channel, ghost-state
    transition logic

Week 9 - Map blockout:
[ ] Block out the 6 zones (Lobby / Self-Intro / Question / Free-Time /
    Interview / Evaluation) in L_GameMap_Default, teleport triggers
    (incl. Free-Time <-> Interview)

Week 10 - Emotes & props:
[ ] 4 emotes (F-you/thumbs-up/thumbs-down/peace) + radial menu, 8 noise
    props

Week 11 - Lobby options & scaling:
[ ] Full pre-lobby settings UI, auto mid-evaluation-count scaling by
    player count

Week 12+ - Sound/art pass, playtesting, Steam store page, QA
    (see doc sections 15-16 for full detail)

=================================================================
DONE THIS SESSION (2026-09-01, round 3, by Claude) - editor restart unlocked
compile-check + playtest, closing the two [ ] items round 2 left open
=================================================================
- User restarted the Unreal Editor, which brought up a live Unreal MCP
  connection (in-process editor control - PIE start/stop, log reading,
  actor queries) that wasn't available to previous sessions. Used it to
  verify round 2's C++-only changes for the first time instead of just
  reading the diff.
- Compile-check: confirmed clean. No LogCompile/LogBlueprint errors on
  editor load, MapCheck 0 Errors/0 Warnings on both Lvl_MainMenu and
  Lvl_Game. The new files from round 2 (jinzzaRoundPhaseSubsystem,
  jinzzaGameGameState, jinzzaPartyPlayerState, jinzzaDisguiseComponent,
  jinzzaGameGameMode/PlayerController changes) all built.
- Playtest (single PIE instance only - this MCP toolset has no multi-client/
  multi-process PIE control, so this does NOT cover the actual lobby ->
  game seamless-travel-with-3-real-players path):
  - Lvl_MainMenu: PIE'd clean, zero Logjinzza warnings/errors.
  - Lvl_Game: PIE'd clean. Confirmed AjinzzaGameGameMode::StartPlay() does
    run and does call AssignRoles() (logged "AssignRoles: need at least 3
    players, got 1 - skipping role assignment" - the designed bail-out path,
    not a crash). Confirmed the DefaultPawnClass fix works: the PIE pawn is
    actually BP_FirstPersonCharacter_C (queried via find_actors), not the
    old bare ADefaultPawn.
- Still NOT verified (needs a real multi-player pass, either multi-process
  PIE with "Number of Players" > 1 in editor Play settings, or an actual
  Steam session with a second machine/account): the 3-player AssignRoles()
  shuffle+RPC path, Client_ReceiveRoleAssignment actually reaching each
  client, and whether AjinzzaGameGameMode::StartPlay() firing after every
  seamlessly-traveled player's pawn/controller exists holds up with real
  seamless travel from Lvl_Lobby (round 2's original concern) - single-PIE
  can't seamless-travel multiple real connections.

=================================================================
DONE THIS SESSION (2026-09-01, round 4, by Claude) - multi-instance PIE
playtest of the 3-player flow, found and fixed a real role-assignment bug
=================================================================
- Set up 3-client PIE via ObjectTools.set_properties on the
  ULevelEditorPlaySettings CDO (playNetMode=PIE_ListenServer,
  playNumberOfClients=3, runUnderOneProcess=true) - the StartPIE MCP tool
  itself has no multi-client option, but it reads these same editor
  play-settings, so this is the same mechanism the editor's own "Number of
  Players" dropdown drives.
- Bug found: PIE'd directly into Lvl_Game with 3 clients. After all 3
  jinzzaPartyPlayerState actors existed in GameState->PlayerArray (checked
  via find_actors/get_properties), Logjinzza still showed "AssignRoles:
  need at least 3 players, got 1 - skipping role assignment" - logged
  earlier, before the other two clients had finished connecting.
  AjinzzaGameGameMode::StartPlay() called RoundPhase->StartRound()
  synchronously, which enters RoleAssignment and calls AssignRoles()
  immediately - racing every remote client's connection, not just a
  seamless-travel corner case. In every real match this would silently
  leave 2 of 3+ players with no role (this was exactly round 2's flagged-
  but-unverified concern - now confirmed real).
- Fix applied (jinzzaGameGameMode.h/.cpp): moved the StartRound() trigger
  out of StartPlay() (which now only binds OnServerPhaseEntered) and into
  a new PostLogin() override that (re)starts a 1.5s debounce timer
  (TryStartRound(), guarded by bRoundStarted) on every login - so the round
  starts once no new player has joined for a beat, rather than on a fixed
  head count that could hang forever if someone disconnects mid-travel.
  Didn't thread an "expected player count" from the lobby through
  GameInstance instead, since that's an extra cross-level plumbing path for
  a number (MaxPlayers is the room's configured *cap*, not who actually
  shows up) that a debounce achieves more simply and more robustly.
- [x] Re-verified after the user compiled (via Hot Reload, not Live Coding -
  LogModuleManager showed UnrealEditor-jinzza-3762.dll reloaded, UBT log
  showed jinzzaGameGameMode.cpp recompiled, "Result: Succeeded"). Re-ran the
  identical 3-client listen-server PIE test on Lvl_Game: confirmed all 3
  jinzzaPartyPlayerState actors present, and - unlike the pre-fix run at the
  same point in the same test - no "AssignRoles: need at least 3 players"
  warning was logged at all this time, on either PIE start or through the
  full 6s warmup. No new errors/asserts/fatals either. Couldn't get a
  positive "AssignRoles succeeded with 3 players" log line as direct
  confirmation, since the fields that would prove it (ServerRole - not even
  a UPROPERTY; FaceType/VoiceFilter/CurrentPhase/LocalRole - private
  UPROPERTYs or plain members) aren't readable through this session's
  object-introspection tool (it only reads *public* reflected properties -
  confirmed by cross-checking against AGameStateBase::PlayerArray, which
  read fine because it's public upstream). The negative result (no
  premature warning where one reliably fired before, across two separate
  pre-fix runs) plus the debounce logic itself being straightforward on
  inspection is good confidence, but if you want a hard positive signal,
  the cheap option is a temporary UE_LOG on AssignRoles' success path.

=================================================================
DONE THIS SESSION (2026-09-02, by Claude) - migrated reusable audio/font
assets from the user's earlier project (docs/Unreal_Game_Noob-main/) into
JINZZA and wired them into the main menu
=================================================================
- The user pointed at a local checkout of their earlier project
  (github.com/KINGWONWOO/Unreal_Game_Noob, extracted to docs/Unreal_Game_Noob-
  main/, now gitignored - see the .gitignore commit earlier today) and asked
  for the menu UI image, BGM, UI sound, and lobby map to be pulled into
  JINZZA and wired into the current levels, plus a list of other things worth
  considering.
- Discovered the extracted folder's .uasset files are Git LFS pointer text
  (100-200 bytes each), not real binary content - the zip download didn't
  include LFS content. Fetched the real binary content directly from
  GitHub's LFS media endpoint (media.githubusercontent.com/media/KINGWONWOO/
  Unreal_Game_Noob/main/<path>) instead, for just the specific files below
  (not a full clone - the repo's git history is large and a full clone timed
  out at 3 minutes).
- Brought over and wired in (now under Content/JINZZA/, matching this
  project's content convention):
  - Content/JINZZA/Audio/Sounds/MainMenu/MainMenuBgm(_Cue) - now plays
    looping on the main menu (UjinzzaMainMenuWidget::NativeOnInitialized,
    stopped in NativeDestruct via a stored UAudioComponent).
  - Content/JINZZA/Audio/Sounds/UISounds/ButtonClickPopSound(_Cue) and
    ButtonHover(_Cue) - now play on every button built via
    JinzzaUI::MakeStyledButton (jinzzaUIStyle.h/.cpp's new
    UJinzzaUIButtonSounds helper, bound to OnClicked/OnHovered), so this
    covers the main menu, lobby, settings, room-settings-kiosk, and
    game-end screens for free via the shared button factory.
  - Content/JINZZA/Audio/Class/SC_Master/SC_Music/SC_SFX/SC_Voice/SCM_Noob -
    brought along only because the Cues above reference SC_Music/SC_SFX as
    their SoundClass; not otherwise wired to anything (this project's own
    audio settings system creates transient SoundClass/SoundMix objects at
    runtime instead - see docs/PROJECT_STATUS.md section 3.4).
  - Content/JINZZA/Fonts/SacheonUju-Regular(_Font)/SacheonUjuOTF-Regular - a
    Korean font, now used for every piece of text built through
    JinzzaUI::TitleFont/HeadingFont/BodyFont (falls back to the engine
    default font if the asset is ever missing), fitting the Korean title
    "진짜를 찾아라" better than the previous default Slate font.
- Deliberately did NOT bring over the menu UI image or the lobby map, and
  flagged this back to the user instead of guessing:
  - Menu UI image: the reference project's UI art (Content/UI/UIImage/
    MainMenu/*) is all branded/themed for that project's own cat-and-dog
    mini-game-hub identity (NoobLogo, memo/sticker paper textures, etc.) -
    none of it fits JINZZA's dark noir/gold/crimson courtroom theme, and
    JINZZA's UI is deliberately texture-free hand-built Slate brushes
    (jinzzaUIStyle.h's header comment). Picking one specific image among
    dozens of mismatched options isn't a call to make silently.
  - Lobby map: the reference project's Content/Levels/Lobby.umap is a full
    level for a different game (fruit/maze/quiz mini-game stalls, NPC
    dialogue, a cat/dog customization flow) with a large, mostly-irrelevant
    dependency tree - wholesale importing it would drag in a lot of dead
    content rather than give JINZZA's own Lvl_Lobby anything it needs.
- [ ] Could not compile-check this session (no build-tool invocation
  exposed, same limitation noted in section 6 of docs/PROJECT_STATUS.md) -
  the changes touch jinzzaUIStyle.h/.cpp (new UJinzzaUIButtonSounds UCLASS)
  and jinzzaMainMenuWidget.h/.cpp (new MusicComponent). Do a Hot Reload/Live
  Coding compile before relying on this.
- [ ] Could not playtest (no input-injection tool, same limitation) - verify
  in PIE that the main menu BGM starts/loops/stops correctly and that button
  click/hover sounds play without being obnoxious at default volume levels.
- [ ] One leftover, harmless stray file: Content/Audio/Sounds/MainMenu/
  MainMenuBgm_Cue.uasset (the original 129-byte LFS-pointer copy) got stuck
  locked by the editor's asset scanner mid-session and couldn't be deleted.
  It's not a registered asset (nothing references it), so it's inert, but
  restarting the editor should release the lock so it can be deleted; don't
  git-add it in the meantime.
- Changes are NOT committed - per usual, commits happen only when
  explicitly requested.

=================================================================
DONE THIS SESSION (2026-09-02, continued, by Claude) - brought over the
lobby BGM and footstep sounds too, on request
=================================================================
- Content/JINZZA/Audio/Sounds/Lobby/LobbyBgm__cut_83sec_(_Cue) - fetched the
  same way as the main menu BGM (real LFS binary content pulled from
  media.githubusercontent.com, not the broken pointer files in the local
  checkout). Wired into UjinzzaLobbyWidget the same way as the main menu
  (loops on NativeOnInitialized, stops on NativeDestruct via a stored
  UAudioComponent) - so it now plays for the whole time a player is in
  Lvl_Lobby.
- This Cue's internal wave reference came in pointing at the old /Game/
  Audio/... path (same LFS-content-vs-project-layout mismatch as the main
  menu Cue last time). This time fixed it directly via the object-property
  tools instead of the move-based workaround: walked the SoundCue's node
  graph (FirstNode -> SoundNodeLooping_0 -> childNodes[0] =
  SoundNodeWavePlayer_0) and patched SoundNodeWavePlayer_0.soundWaveAssetPtr
  to point at the asset's real new path, then saved. Confirmed clean via
  get_dependencies afterward - faster and less fragile than last session's
  repeated move-away-and-back dance, worth using again for any future stale-
  Cue-reference fix.
- Content/JINZZA/Audio/Sounds/FootStep/Footstep and FootstepGrass (plain
  SoundWave assets, no dependencies, no path issues) are in and usable
  directly as USoundBase.
- Content/JINZZA/Audio/Sounds/FootStep/Cue_Footstep came over but its
  FirstNode is None - it's an empty, never-finished Cue in the source
  project itself (not something this session broke), so it doesn't play
  anything as-is. Left it in place rather than guessing at how to reconstruct
  a Random-node wave-variation graph through the property tools. If you want
  footstep audio, either use Footstep/FootstepGrass directly (e.g. pick one
  in code, or alternate randomly) or open Cue_Footstep in the Sound Cue
  editor and wire the two waves into a Random node by hand - a couple-minute
  manual job, not something worth scripting.
- Did NOT add any footstep-trigger gameplay logic (no AnimNotify or
  movement-tick hook exists yet to play these on) - bringing the assets over
  was the ask; wiring up when/how footsteps actually fire is a separate,
  undecided feature (walk vs. sprint volume, distance-based vs. anim-notify-
  based) - ask if you want that built.
- [ ] Same as above: not compiled or playtested this session either
  (jinzzaLobbyWidget.h/.cpp changes need a Hot Reload/Live Coding pass).
- Changes are NOT committed.

=================================================================
DONE THIS SESSION (2026-09-02, continued, by Claude) - checked whether last
session's C++ changes had actually taken effect, then restructured the
Settings and Lobby screens' layout after the reference project's Option.png
=================================================================
- User asked to "actually apply" the noob-game migration and fix anything
  needed. Checked the live editor's log directly (LogModuleManager/
  UnrealBuildTool entries) - confirmed no compile has happened since last
  session's jinzzaUIStyle/jinzzaMainMenuWidget/jinzzaLobbyWidget edits, so
  none of that code is live in the running editor yet. This session has no
  exposed build-trigger tool (confirmed again, same as every prior session -
  see section 6 of docs/PROJECT_STATUS.md), so a Hot Reload/Live Coding
  compile from you is still the blocking step before any of it can be
  playtested or fixed against real errors.
- Tried to fix the other flagged loose end (Cue_Footstep's empty node graph)
  by attempting to construct a SoundNodeRandom via the object-property
  tools' "instanced sub-object" path. It reported success but didn't
  actually create the node (read back as still None afterward) - backed off
  rather than risk corrupting the asset via an unsupported operation.
  Unchanged from last session's recommendation: wire it by hand in the
  Sound Cue editor, or use Footstep/FootstepGrass directly in code.
- User then asked to "bring the setting design and the lobby design" too.
  Clarified via question: wanted the reference project's Settings/Lobby
  screen LAYOUT/STRUCTURE recreated using JINZZA's own noir/gold style, not
  the reference project's actual images (consistent with the menu-image/
  lobby-map calls from earlier this session).
- Found docs/Unreal_Game_Noob-main (1)/Unreal_Game_Noob-main/Doc/Images/
  Option.png and Lobby.png (real screenshots in that project's own repo,
  not something this session generated) and read them directly to see the
  actual layout, since the noob project isn't open in this editor session
  and its UMG Widget Blueprints can't be inspected any other way.
  - Option.png: a left vertical sidebar of icon+label tabs (Video/Audio/
    Controls/Etc), a right content area of labeled slider rows for the
    active tab, and a bottom-right Apply/Cancel button pair.
  - Lobby.png turned out to be a 3D room screenshot, not a UI screen - there
    is no separate lobby UI mockup in that project's docs. Interpreted
    "lobby design" as the existing JINZZA lobby info panel's own structure
    (per the clarifying answer), not the 3D room.
- jinzzaSettingsWidget.h/.cpp: restructured from a top horizontal tab bar
  into a left sidebar (fixed 160px width) + right content column + bottom-
  right Apply/Back, matching Option.png's arrangement. No images used - the
  "active tab" cue from Option.png's highlighted ribbon is reproduced as a
  small JinzzaUI::Color_Accent bar next to the active sidebar tab (new
  SetActiveTab() helper toggles it alongside the WidgetSwitcher index; new
  TabAccentBars array holds the four bars). All 4 tabs' actual content
  (BuildGraphicsPage/BuildAudioPage/BuildControlsPage/BuildGameplayPage) is
  untouched - only the outer chrome changed.
- jinzzaLobbyWidget.h/.cpp: added a lighter structural pass - grouped the
  existing fields under "Room" (settings summary + player count) and
  "Actions" (Invite/Start buttons) headings with a divider under the title,
  instead of one flat stack. PlayerCountText changed from
  JinzzaUI::MakeSectionHeading to MakeBodyText since it now sits under its
  own "Room" heading rather than needing to read as a heading itself.
- [ ] Not compiled or playtested (see above - still blocked on your Live
  Coding/Hot Reload trigger). Once compiled, should verify: sidebar tab
  switching still shows the right accent bar, all Settings controls still
  read/write the same UjinzzaGameUserSettings calls as before (only the
  chrome moved, not the Apply logic), and the lobby panel's new headings
  don't crowd the existing "Press E" interaction prompt or overflow at
  smaller resolutions.
- Changes are NOT committed.

=================================================================
DONE THIS SESSION (2026-09-02, continued, by Claude) - user compiled, so
actually playtested the accumulated BGM/UI-sound/font/layout changes
=================================================================
- Confirmed the compile actually happened this time (LogModuleManager/
  HotReload log entries for two separate compiles, ~13:04-13:09 and
  ~13:24-13:25, both clean - no compile errors in either).
- Found the live editor still had a leftover 3-client listen-server PIE
  config from an earlier session (round 4's multi-client bug-hunt, see the
  2026-09-01 entries above) - the very first PIE test this round silently
  launched 3 clients instead of 1, which is why the first viewport capture
  came back looking wrong. Reset ULevelEditorPlaySettings back to
  PIE_Standalone/1 client before re-testing.
- Viewport screenshots (CaptureViewport) turned out to be unusable this
  session: the base64 PNG payload is large enough (~10-25 KB) that manually
  copying it through a text write introduced transcription corruption (one
  attempt decoded to a valid but content-wise-black PNG - actually the
  wrong capture target, a separate issue - and a second attempt didn't even
  decode as valid base64). Didn't chase a fix - fell back entirely to log-
  and property-based verification instead, which is more reliable anyway
  for confirming code correctness (just not pixel layout).
- What was actually verified, single-client, single-instance PIE for each:
  - Lvl_MainMenu: PIE clean, zero errors. Bumped LogAudio to Verbose and
    confirmed via the log: "Playing AudioComponent ... with Sound:
    'MainMenuBgm_Cue'" - the main menu BGM is genuinely playing, not just
    compiling.
  - Lvl_Lobby (loaded directly rather than via Host Game, since there's
    still no input-injection tool to click through the main menu):
    PIE clean, zero errors, MapCheck 0/0. Same LogAudio confirmation:
    "Playing AudioComponent ... with Sound: 'LobbyBgm__cut_83sec__Cue'" -
    lobby BGM also genuinely playing. The restructured Room/Actions-
    grouped panel constructs without error (NativeOnInitialized doesn't
    assert/crash).
  - Settings sidebar (the Option.png-modeled restructure): compiles clean
    and constructs without error by the same reasoning (WidgetSwitcher's
    pages are built unconditionally in NativeOnInitialized on both the
    Graphics/Audio/Controls/Gameplay content AND the sidebar chrome, so a
    crash there would show up as an error on any main-menu PIE run) - but
    could NOT be reached/clicked to confirm the sidebar accent-bar actually
    moves between tabs, since it's nested behind the main menu's Settings
    button and there's no way to click it programmatically. Genuinely
    unverified beyond "doesn't crash to construct."
  - UI click/hover sounds (ButtonClickPopSound/ButtonHover): same
    limitation - the Cue asset references were already confirmed clean via
    get_dependencies, and the binding code compiles, but actually hearing
    them requires a real click, which isn't available to this session.
- Noticed (not caused by anything this session touched) a stale log entry
  from ~13:10 today, before any of today's compiles: a
  "UEngine::TickWorldTravel failed to Handle server travel to URL: /Game/
  JINZZA/Level/Lvl_Lobby?listen" ensure-failure, right after the first
  hot reload. Did not reproduce it on this session's clean single-client
  Lvl_Lobby load. Flagging in case you saw a real Host Game -> Lobby
  failure while testing yourself - if so, worth a dedicated repro session,
  but nothing in tonight's changes touches ServerTravel/GameInstance code,
  so it's unlikely to be related to any of today's edits.
- Changes are still NOT committed - ask if you want this committed and
  pushed now that it's been playtested clean.

=================================================================
DONE THIS SESSION (2026-09-07, by Claude) - fixed the PIE-blocking widget-binding
compile errors by having 5 widget classes build their own UMG tree in C++
=================================================================
- Symptom: PIE failed to compile with `LogBlueprint: Error: ... A required widget
  binding "X" ... was not found` on WBP_MainMenu, WBP_Settings, WBP_Lobby,
  WBP_RoomSettings, and WBP_GameEnd - none of their Designer trees were ever
  authored (confirmed via Saved/Logs/jinzza.log), and no unreal-mcp tool exists
  that can place child widgets into a WBP's visual tree (still true - checked
  BlueprintTools/ObjectTools again this session, nothing new).
- User asked to fix it via C++ now rather than hand-placing widgets in the
  Designer, with the explicit plan to swap back to Designer-authored BindWidget
  layouts later, once the real UI design/art pass happens.
- Wrote docs/umg_widget_authoring_guide.md first (the full per-widget required-
  binding list + suggested layout + JinzzaUIStyle color/font values, for that
  later Designer pass), then implemented the C++ route on top of it:
  - Added `JinzzaUI::MakeLabeledRow` to jinzzaUIStyle.h/.cpp - a reusable
    "fixed-width muted label + control" row, since Settings/RoomSettings both
    needed dozens of these.
  - jinzzaGameEndWidget, jinzzaLobbyWidget, jinzzaRoomSettingsWidget,
    jinzzaMainMenuWidget, jinzzaSettingsWidget: removed `meta = (BindWidget)`
    from every property (so the Blueprint compiler no longer requires anything
    to exist in the WBP's Designer tree) and added a `BuildWidgetTree()`,
    called first thing in `NativeOnInitialized()`, that constructs the entire
    tree via `WidgetTree->ConstructWidget<>()` + the `JinzzaUI::Make*` helpers
    and assigns the results straight to the member pointers. Verified first
    (by reading every .cpp fully) that every click handler, combobox option
    list, and slider/spinbox range is already wired in code (Populate*Page
    etc.) - none of that needed touching, so this was purely a tree-
    construction addition, not a logic change.
  - MainMenu's SettingsWidget page is now a directly-constructed
    UjinzzaSettingsWidget instance (WidgetTree->ConstructWidget), not a loaded
    WBP_Settings class - consistent since Settings builds its own tree too.
  - CustomizationButton/CustomizationWidget/CharacterPreviewImage/
    VoiceTestWidget (MainMenu's BindWidgetOptional fields) are left
    unconstructed/null on purpose - their backing content doesn't exist yet
    and the existing code already null-checks all four.
  - Each header's class comment now says "TEMP C++-built" and spells out the
    exact revert steps (delete BuildWidgetTree(), restore the BindWidget meta,
    lay it out for real) so a future session/you doesn't mistake this for the
    permanent architecture.
- [ ] NOT compiled or playtested this session (no build-trigger tool, as
  always - see docs/PROJECT_STATUS.md §6). This is a substantial change across
  5 files - please run a Live Coding compile (or full rebuild) and check
  Saved/Logs/jinzza.log for the widget-binding errors clearing, then a PIE
  pass on Lvl_MainMenu (Host Game -> Settings -> each tab) and Lvl_Lobby before
  trusting it.
- Changes are NOT committed.
- docs/umg_widget_authoring_guide.md's intro now flags it as "superseded for
  now" and doubles as documentation of what BuildWidgetTree() actually builds,
  for whenever the Designer swap-back happens.

=================================================================
DONE THIS SESSION (2026-09-07, continued, by Claude) - found and fixed why
movement/jump were never confirmed working, added Sprint, verified Settings
really drive gameplay
=================================================================
- User asked to confirm WASD movement/interactions work in Lvl_Lobby and
  Lvl_Game, implement Jump/Sprint if not already real, and verify Settings
  changes actually affect gameplay (not just stored prefs).
- Root cause found (this is the real finding, not a guess): AjinzzaLobbyPlayerController
  and AjinzzaGamePlayerController - the PlayerControllerClass each GameMode
  actually spawns - derived straight from the engine's bare APlayerController,
  NOT from AjinzzaPlayerController (the class whose SetupInputComponent()
  actually calls Subsystem->AddMappingContext(...)). Confirmed via
  ObjectTools: IMC_Default/IMC_MouseLook are correctly configured with real
  key mappings (get_dependencies showed IMC_Default depending on
  IA_Move/IA_Jump/IA_Look/IA_Interact/etc, so the data assets themselves are
  fine) and BP_FirstPersonPlayerController (a leftover, unused-by-any-GameMode
  Blueprint) has them wired into DefaultMappingContexts - but neither Lobby
  nor Game's actual controller ever added ANY mapping context to the Enhanced
  Input subsystem. Net effect: WASD/Look/Jump were silently 100% dead in both
  Lvl_Lobby and Lvl_Game, not just untested - this was never going to show up
  in any of the prior sessions' PIE checks since those only verified BGM/
  compile-cleanliness/role-assignment logs, never actual movement.
  - Fix: AjinzzaLobbyPlayerController and AjinzzaGamePlayerController now
    derive from AjinzzaPlayerController instead of APlayerController (both
    already called Super::BeginPlay(), and Lobby already called
    Super::SetupInputComponent(), so this was a one-line parent-class swap
    each plus the header include). AjinzzaPlayerController's constructor now
    also hardcodes ConstructorHelpers::FObjectFinder defaults for IMC_Default/
    IMC_Sprint (DefaultMappingContexts) and IMC_MouseLook
    (MobileExcludedMappingContexts), since neither Lobby nor Game controller
    has a Blueprint asset of its own to configure these from a Class Defaults
    panel - matches the existing DefaultPawnClass ConstructorHelpers fallback
    pattern already used in both GameModes.
- Jump: was already properly implemented (JumpAction bound to
  DoJumpStart/DoJumpEnd -> Character's built-in Jump()/StopJumping()) - just
  unreachable in practice because of the bug above.
- Sprint: did NOT exist at all (no SprintAction, no speed-changing code, and
  no IA_Sprint asset in JINZZA's own Input/Actions folder - only an unrelated
  leftover in Content/JINZZA/Variant_Horror/Input/ tied to the unused
  Horror-variant template character). Implemented:
  - AjinzzaCharacter: new SprintAction (held IA_Sprint), SprintSpeedMultiplier
    (default 1.6x, EditAnywhere), DoSprintStart/DoSprintEnd toggling
    GetCharacterMovement()->MaxWalkSpeed relative to BaseWalkSpeed (captured
    once in BeginPlay, so it scales whatever the Blueprint has configured
    rather than a hardcoded number). Guarded by bStunned like DoJumpStart;
    OnRep_Stunned also cancels an in-progress sprint's speed boost.
  - Asset side (done via the live editor's object/asset tools, not by hand):
    moved (not duplicated - a real engine reference-safe rename, so nothing
    broke) Variant_Horror's IA_Sprint -> /Game/JINZZA/Input/Actions/IA_Sprint,
    and its owning IMC_Horror -> /Game/JINZZA/Input/IMC_Sprint (renamed since
    it's not Horror-specific content anymore, it's the one real Sprint
    mapping). Verified via get_dependencies/get_referencers that the rename
    fixup kept IMC_Sprint's mapping pointing at the moved IA_Sprint correctly.
  - [ ] Not verified which physical key IMC_Sprint's mapping actually uses
    (the Mappings array is a struct-of-structs that this session's object-
    property tools can't reliably read back, confirmed - get_properties
    returns "Mappings": [] even though get_dependencies proves real content
    exists). Almost certainly Left Shift (stock template convention) - please
    open IMC_Sprint in the editor once to confirm/adjust.
  - [ ] After you compile: BP_FirstPersonCharacter's new SprintAction
    property still needs to be set to /Game/JINZZA/Input/Actions/IA_Sprint
    (same pattern as its existing JumpAction/MoveAction/etc.) - couldn't do
    this yet since the property doesn't exist in the reflection system until
    the C++ compiles. Ask Claude to do it via ObjectTools once compiled, or
    set it by hand in the Blueprint's Class Defaults.
- Settings-affects-real-gameplay verification (what was actually checked, not
  assumed):
  - Mouse Sensitivity + Invert Y: confirmed real - AjinzzaCharacter::DoAim
    reads UjinzzaGameUserSettings::Get() every frame and scales/flips the aim
    input directly. Genuinely wired, not just stored.
  - Key rebinding: confirmed real and generic -
    AjinzzaPlayerController::BuildRuntimeMappingContext looks up
    Settings->GetKeyRebind(Mapping.Action->GetFName()) for every mapping on
    whatever IMCs are actually in DefaultMappingContexts, so it isn't
    hardcoded to the 4 rows Settings happens to expose. Jump's rebind row is
    genuinely functional (IA_Jump matches). Sprint's rebind row will become
    functional once the two follow-ups above land (name already matches:
    "IA_Sprint").
  - Found a real gap, not fixed (a product decision, not mine to make
    unilaterally): Settings' Shoot/SwapWeapon rebind rows reference
    IA_Shoot/IA_SwapWeapon, which are Variant_Shooter template leftovers -
    AjinzzaCharacter never binds either action at all. Rebinding those two
    rows currently does nothing in the real game. Design doc has no weapon
    system, so these are almost certainly dead UI to remove - flagging for a
    decision rather than deleting the rows myself.
- [ ] NOT compiled or playtested this session (no build-trigger tool, as
  always). This is a substantial change (2 controller base classes, new
  Character input action, a constructor-level asset dependency) - please run
  a Live Coding compile and check Saved/Logs/jinzza.log is clean, then
  actually walk a character around in PIE for both Lvl_Lobby and Lvl_Game
  (WASD, Space to jump, Left Shift to sprint) before trusting this fixes what
  it's supposed to.
- Changes are NOT committed.

=================================================================
DONE THIS SESSION (2026-09-07, continued, by Claude) - user confirmed WASD
works but Sprint key does nothing; reworked lobby mouse/UI flow into a
kiosk-driven model and added a real Steam friends-list invite panel
=================================================================
- Sprint still not confirmed working - user reports the key does nothing.
  SprintInputAction/BP_FirstPersonCharacter wiring is confirmed correct (read
  back cleanly via ObjectTools after the compile). Two remaining suspects,
  neither confirmed yet: (1) IMC_Sprint's actual mapped key - still can't be
  read via this session's tools (Mappings is a struct array that get_properties
  can't serialize), asked the user to open it in the editor and check/report;
  (2) they may have tested in a PIE session from before the SprintInputAction
  property was set on the Blueprint CDO - needs a fresh PIE launch.
- User's real complaint: the always-on top-left lobby panel (Invite Friends +
  Start Match) forces the mouse cursor visible/UI-focused just to reach two
  buttons, fighting with FPS mouse-look. Root cause found while fixing it:
  AjinzzaRoomSettingsKiosk/AjinzzaWardrobeKiosk only ever set
  bShowMouseCursor = true on open and NEVER turned it back off on close - the
  lobby's one-time BeginPlay GameAndUI+cursor-visible setup was the only thing
  making the cursor usable at all, and nothing ever restored Game-only mode.
- Fix, confirmed via user's answer to a clarifying question (Start Match
  folds into a kiosk too, consistent with the existing interaction model):
  - AjinzzaInteractableKiosk (new .cpp - didn't exist before, base class was
    pure-virtual-only): added static EnterKioskUIMode/ExitKioskUIMode helpers
    (cursor+SetInputMode together, both directions). RoomSettingsKiosk and
    WardrobeKiosk now call these instead of only setting bShowMouseCursor.
  - AjinzzaLobbyPlayerController::BeginPlay now defaults to hidden-cursor/
    Game-only input (was GameAndUI + cursor visible + LobbyWidget focused) -
    WASD look/move work immediately on entering the lobby now.
  - UjinzzaLobbyWidget: removed InviteButton/StartButton and their click
    handlers entirely - it's a pure display HUD now (room settings text,
    player count, the bottom-center interact prompt).
  - New AjinzzaStartMatchKiosk: no panel, just server-travels to Lvl_Game
    immediately on E (host-only, same PC->HasAuthority() check the old button
    used, just relocated).
  - New AjinzzaFriendInviteKiosk + UjinzzaFriendInviteWidget: opens a
    centered panel (same C++-built-tree pattern as this session's other
    widgets) showing the local player's real Steam friends list, one row per
    friend (name + an Invite button on its right via JinzzaUI::MakeLabeledRow,
    disabled for offline friends), plus Refresh and Close. Per-row Invite
    click uses a small UJinzzaFriendInviteRowHandler UObject (mirrors
    UJinzzaUIButtonSounds's role) since UButton::OnClicked is zero-arg and
    can't carry which friend was clicked.
  - UjinzzaGameInstance: added RequestFriendsList()
    (IOnlineFriends::ReadFriendsList/GetFriendsList -> OnFriendsListReceived
    delegate) and InviteFriendToSession(NetIdString)
    (IOnlineIdentity::CreateUniquePlayerId + IOnlineSession::
    SendSessionInviteToFriend) - a real per-friend invite, not just Steam's
    generic overlay dialog (InviteFriends()/ShowInviteUI left as-is, unused
    by the new widget but harmless).
- [ ] NOT compiled yet. After compile: still need to actually PLACE
  AjinzzaFriendInviteKiosk and AjinzzaStartMatchKiosk into Lvl_Lobby (checked
  the live level first - only one kiosk, jinzzaRoomSettingsKiosk_0, is
  actually placed at (0,0,80); AjinzzaWardrobeKiosk isn't placed in the level
  at all despite existing in code, a pre-existing gap this session didn't
  cause or fix). Can't spawn the two new classes via SceneTools until they
  exist in the compiled reflection system.
- [ ] Also not yet placed/tested: the Sprint key question above.
- Changes are NOT committed.

=================================================================
DONE THIS SESSION (2026-09-08, by Claude) - full TEMP audio pass: footsteps,
jump/land, per-prop use sounds, and a Lvl_Game BGM, all placeholder content
per the user's explicit "add everything you can, temporarily, I'll replace
later" ask
=================================================================
- Fetched real placeholder SFX/BGM from the same source this project already
  draws its other temp audio from (github.com/KINGWONWOO/Unreal_Game_Noob) -
  same method as the 2026-09-02 sessions (the local docs/Unreal_Game_Noob-main
  checkout's .uasset files are still Git LFS pointer text, so fetched the real
  binaries directly from media.githubusercontent.com/media/KINGWONWOO/
  Unreal_Game_Noob/main/<path> and copied them straight into Content/JINZZA/
  Audio/Sounds/<Category>/, keeping each file's original name so no internal
  package-name mismatch is introduced). All plain SoundWave assets (no _Cue
  wrapper, no dependencies) - confirmed via get_dependencies, same "no path
  issues" property the existing Footstep/FootstepGrass assets already had:
  - Audio/Sounds/Bat/Punch1 (Combat/Punch1) - Bat's UseSound
  - Audio/Sounds/Boombox/FruitGameBgm (fruitGame/FruitGameBgm) - Boombox's Track
  - Audio/Sounds/Megaphone/PressButton (fruitGame/PressButton) - Megaphone's UseSound
  - Audio/Sounds/Basketball/InteractWoodenBox__cut_0sec_ (fruitGame/InteractWoodenBox)
    - Basketball's UseSound, also reused as the Character's LandSound (see below)
  - Audio/Sounds/BasketballHoop/correctanswer (fruitGame/correctanswer) - Hoop's ScoreSound
  - Audio/Sounds/StunGun/Stunned (Combat/Stunned) - StunGun's UseSound
  - Audio/Sounds/Game/QuizGameBgm (QuizGame/QuizGameBgm) - new Lvl_Game BGM,
    set bLooping=true directly on the SoundWave via ObjectTools (no Cue needed
    for a simple loop)
  All 6 prop Blueprints' CDOs (BP_Bat/BP_Boombox/BP_Megaphone/BP_Basketball/
  BP_BasketballHoop/BP_StunGun) already had these exact USoundBase fields
  wired to UGameplayStatics::PlaySoundAtLocation in C++ from earlier sessions
  (AjinzzaInteractableProp::Multicast_PlayEffects, AjinzzaBasketballHoop::
  Multicast_PlayScoreEffects, AjinzzaBoomboxProp's AudioComponent) - just
  unassigned until now, so this was pure asset-assignment via ObjectTools'
  set_properties on each Default__X_C, no C++ changes needed for props.
- New C++ (AjinzzaCharacter, jinzzaCharacter.h/.cpp): footsteps didn't exist
  in any form before (todo.txt's 2026-09-02 entry explicitly flagged this as
  not built). Added FootstepSound/FootstepSoundAlt (defaults to the existing
  Footstep/FootstepGrass assets, randomly alternated), FootstepDistanceInterval
  (150cm stride), ticked every instance (not just locally-controlled - this is
  cosmetic and replicated movement drives a simulated proxy's velocity too, so
  nearby players hear each other's footsteps with no RPC) via new
  UpdateFootsteps(), called unconditionally from Tick(). Also added JumpSound
  (defaults to the existing ButtonClickPopSound - played in DoJumpStart only
  when CanJump() is true) and LandSound (defaults to the new
  InteractWoodenBox thud - played from a new Landed() override).
- New C++ (AjinzzaGamePlayerController): Lvl_Game had no BGM at all (only
  MainMenu/Lobby did). Added a MusicComponent, started in BeginPlay (same
  IsLocalController()-gated block that already creates the GameEndWidget) via
  LoadObject<USoundBase> + UGameplayStatics::SpawnSound2D on QuizGameBgm,
  stopped in a new EndPlay() override - exact same pattern as
  UjinzzaMainMenuWidget/UjinzzaLobbyWidget's BGM (NativeOnInitialized/
  NativeDestruct), just on a PlayerController instead of a widget since
  Lvl_Game has no persistent menu-style widget to hang it off.
- Deliberately NOT touched (would need their own new C++ hooks, out of scope
  for "add what you can" without extra design calls):
  - Kiosk panel-open sound (RoomSettings/Wardrobe/FriendInvite/StartMatch) -
    the reference project has a LobbyPannelOpen sound that would fit, but
    wiring it means picking a call site in AjinzzaInteractableKiosk's
    EnterKioskUIMode - flagging as a good next step, not done automatically.
  - Emote montages, prop UsageIcons, BasketballHoop's ScoreEffect (Niagara) -
    still genuinely blocked on missing animation/VFX content, not audio.
- [ ] NOT compiled or playtested this session (no build-trigger tool, as
  always - see docs/PROJECT_STATUS.md §6). Touches jinzzaCharacter.h/.cpp and
  jinzzaGamePlayerController.h/.cpp - please run a Live Coding compile, then
  check Saved/Logs/jinzza.log is clean and walk around in PIE (Lvl_Lobby or
  Lvl_Game) to confirm footsteps/jump/land play and don't sound obnoxious at
  default volumes, and test each prop's F-key/use interaction for its sound.
- Changes are NOT committed.

=================================================================
DONE THIS SESSION (2026-09-08, continued, by Claude) - fixed a real build
failure the user hit after compiling the changes below
=================================================================
- User ran a Live Coding build and got a C2084/C2572 redefinition error in
  "Module.jinzza.cpp" (the unity blob) between jinzzaRoomSettingsWidget.cpp
  and jinzzaFriendInviteWidget.cpp - both had their own identically-signed
  `AddSpaced(UVerticalBox*, UWidget*, float)` helper in an anonymous
  namespace. Root cause: anonymous namespaces only guarantee uniqueness per
  translation unit, and Unreal's adaptive-non-unity build pulled this
  session's 5 changed files OUT of the unity blobs (per-file compile,
  confirmed clean individually - steps 2-6 all succeeded), which reshuffled
  which unchanged files land in the same blob together - putting
  RoomSettingsWidget.cpp and FriendInviteWidget.cpp in the same one for the
  first time and surfacing a pre-existing latent collision neither file had
  hit before. Grepped and found jinzzaLobbyWidget.cpp and
  jinzzaCustomizationWidget.cpp (both edited this session) had each grown
  their own third and fourth copies of the exact same helper - a ticking
  time bomb regardless of what triggered it this time.
- Fix: moved `AddSpaced` into the shared `JinzzaUI` namespace
  (jinzzaUIStyle.h/.cpp, right after MakeLabeledRow) as the one real
  definition, and deleted the four per-file anonymous-namespace copies
  (jinzzaRoomSettingsWidget.cpp, jinzzaFriendInviteWidget.cpp,
  jinzzaLobbyWidget.cpp, jinzzaCustomizationWidget.cpp), updating every call
  site to `JinzzaUI::AddSpaced(...)`. UjinzzaMainMenuWidget's own AddSpaced is
  a local lambda captured inside BuildWidgetTree (not a free function), so it
  was never part of this collision and needed no change. Behavior is
  otherwise identical - the only real difference is the default TopPadding
  argument value used by the couple of call sites in each file that didn't
  pass one explicitly is now uniformly 10.f (was already 10.f in Room
  Settings/FriendInvite/Customization; only Lobby's had used 8.f - the two
  divider-spacing call sites affected are +2px, cosmetically invisible).
- [ ] Still not compiled by this session (no build-trigger tool) - please
  re-run the Live Coding build. This should now get past Module.jinzza.cpp;
  if anything else fails, paste the new error.
- Changes are NOT committed.

=================================================================
DONE THIS SESSION (2026-09-08, continued, by Claude) - placed the last lobby
kiosk, gave Customization a real UMG tree, added a main-menu Customize button
+ placeholder logo, and a first-open welcome overlay in the lobby
=================================================================
- User asked to: invite/start-match-test the lobby kiosks, place the
  "customizable object" in Lvl_Lobby, confirm the mouse-visible/keyboard-input
  toggle on opening/closing a kiosk UI, show an intro when the lobby first
  opens, and add a Customization button (right side of the main menu) with a
  temporary placeholder for a company logo the user will supply later.
- Checked Lvl_Lobby first: AjinzzaFriendInviteKiosk and AjinzzaStartMatchKiosk
  were already placed (from the 2026-09-07 session, once compiled) at
  (-400,0,80)/(400,0,80) either side of the existing RoomSettingsKiosk at
  (0,0,80) - only AjinzzaWardrobeKiosk ("the customizable object") was still
  missing. Spawned it at (0,400,80) via SceneTools and saved the level.
- Mouse-visible/keyboard-input toggle: already fully implemented (confirmed by
  reading, not assumed) - every kiosk (RoomSettings/Wardrobe/FriendInvite)
  calls AjinzzaInteractableKiosk::EnterKioskUIMode on open (cursor visible,
  Game+UI input, keyboard focus on the panel) and ExitKioskUIMode on close
  (cursor hidden, Game-only input) - see that class's 2026-09-07 header
  comment. Nothing needed changing here, just verifying it covers the newly-
  placed Wardrobe kiosk too (it does, same base class).
- Found a real gap while wiring the Customization button: UjinzzaCustomizationWidget
  was the one main-menu-adjacent widget the 2026-09-07 "5 widgets get a
  BuildWidgetTree()" pass had NOT touched - it was still UMG-authored
  (BindWidgetOptional, no Designer layout ever built, same "no unreal-mcp
  tool can place child widgets in a WBP" gap as always). Adding a working
  Customization button meant it would open a genuinely blank panel otherwise,
  so gave it the same BuildWidgetTree() treatment as
  UjinzzaSettingsWidget/UjinzzaRoomSettingsWidget: 5 Prev/Value/Next cycle rows
  (Head/HairColor/Top/Eyebrows/Eyes, HairColor's row also gets its color
  swatch image) plus a Done button, all via JinzzaUI helpers - no logic
  changed, RefreshAllRows/OnXPrevClicked/etc. were already complete and just
  needed real widgets to bind to.
- jinzzaMainMenuWidget.cpp: constructed CustomizationButton (right side of the
  button page, independent of the centered Host/Settings/Quit stack) and
  CustomizationWidget (Switcher page 2) for real - both were previously
  declared but deliberately left null. The click handler/back-navigation
  wiring in NativeOnInitialized already existed and needed no changes.
  Wrapped Switcher in a new RootOverlay so a top-left "LOGO" text badge (via
  JinzzaUI::MakePanelBackground) can sit on top of every switcher page as a
  TEMP stand-in for the company logo the user said they'll provide later -
  swap that badge for a UImage pointed at the real texture when it exists.
- jinzzaLobbyWidget.h/.cpp: added a first-open welcome overlay (full-screen
  dim scrim + centered panel: "JINZZA" title, a short orientation blurb about
  the kiosks, and a "closes automatically" hint) built in BuildWidgetTree,
  shown immediately and auto-hidden via a 5-second FTimerHandle set in
  NativeOnInitialized (cleared in NativeDestruct). Deliberately dismiss-by-
  timer only, no input-driven skip - simplest fit for "show it when the lobby
  first opens" without adding new input plumbing.
- [ ] NOT compiled or playtested this session either (same standing
  limitation). New/changed files: jinzzaCustomizationWidget.h/.cpp,
  jinzzaMainMenuWidget.cpp, jinzzaLobbyWidget.h/.cpp. After compiling: PIE the
  main menu and click Customize (right side) to confirm the 5 rows + Done
  button actually work and the logo badge reads fine top-left; PIE Lvl_Lobby
  to confirm the welcome overlay shows on open and clears after ~5s; walk up
  to the Wardrobe kiosk (0,400,80) and press E to confirm it opens the same
  Customization panel with the cursor toggle working.
- Changes are NOT committed.

=================================================================
DONE THIS SESSION (2026-09-08, continued, by Claude) - worked through
docs/JINZZA_todo.xlsx (a 212-row project tracker, Korean 완료/부분완료/예정
status columns) and closed out everything genuinely finishable right now -
skipped anything blocked on external accounts, real art/animation content, or
undecided game-design questions (2-judge mode, host-picks UI, candidate-vote
flow, etc. - all already flagged as such in the sheet itself)
=================================================================
- Removed the AssignRoles() [TEMP DEBUG] success-path log (jinzzaGameGameMode.cpp)
  - the sheet's own §13 QA row asked for this; the PostLogin/TryStartRound fix
  it was guarding has been stable since 2026-09-01 round 4.
- Removed the dead Shoot/SwapWeapon key-rebind rows from Settings' Controls tab
  (jinzzaSettingsWidget.h/.cpp) - IA_Shoot/IA_SwapWeapon are Variant_Shooter
  template leftovers AjinzzaCharacter never binds (flagged 2026-09-07, this
  project has no weapon system). Jump/Sprint rebind rows untouched.
- Wired Question Time's cycle count to a real lobby setting instead of a
  hardcoded 2 (design doc §9's own default): added
  FJinzzaMatchSettings::QuestionTimeCycles (1-3), a new spin-box row in
  AjinzzaRoomSettingsKiosk's panel (jinzzaRoomSettingsWidget.h/.cpp, mirrors
  VoteCount's row exactly), read by
  UjinzzaRoundPhaseSubsystem::GetPhaseDurationSeconds (65s/cycle - the doc's
  own 작성20s+답변15s+토론30s math), and surfaced in the lobby's room-info text.
- Checked docs/JINZZA_todo.xlsx's "Sprint 키 동작 검증" concern: BP_FirstPersonCharacter's
  SprintInputAction and IMC_Sprint's IA_Sprint dependency are both still
  correctly wired (re-verified via ObjectTools/get_dependencies) - the wiring
  is not the problem. IMC_Sprint's actual mapped key still can't be read
  through this session's tools (Mappings is a struct array get_properties
  returns as [] - same limitation as 2026-09-07) and blindly overwriting it
  risks corrupting or wiping a config this session can't verify first, so
  deliberately NOT touched. [ ] Still needs the user to open IMC_Sprint in
  the editor once and confirm/fix the mapped key - unchanged ask from
  2026-09-07.
- Confirmed already-satisfied (no code needed) rather than duplicating work:
  - "설정 화면 탭 전환음" (Settings tab-switch sound) and "커스터마이징 아이템
    변경음" (Customization item-change sound) - both the sidebar tab buttons
    and the Customization Prev/Next buttons are already built via
    JinzzaUI::MakeSecondaryButton, which auto-attaches a click/hover sound
    for every button it makes (2026-09-02) - adding another explicit sound
    call would just double it up.
  - "UCharacterCustomizationComponent 구현" (row84) - already exists and is
    already functional (jinzzaCharacterCustomizationComponent.h/.cpp, reads
    UjinzzaGameUserSettings, degrades to a no-op only for the parts with no
    real content yet) - the spreadsheet is stale here, predating that class.
- Added TEMP placeholder one-shot SFX at every "예정" sound hook that already
  has a real code path to hang off of (all reuse assets already in the
  project, plus 2 new ones fetched the same way as the 2026-09-08 audio pass
  - LobbyPannelOpen__cut_1sec_ and WrongAnswerSound, both from the same
  Unreal_Game_Noob reference repo, copied to Content/JINZZA/Audio/Sounds/
  UISounds/ and /Emotes/ respectively, confirmed plain SoundWaves with no
  dependencies same as every other temp asset so far):
  - Main menu entry (NativeOnInitialized) and opening Settings/Customization
    from it (jinzzaMainMenuWidget.cpp - new shared PlayPanelOpenSound helper)
  - Lobby entry (jinzzaLobbyWidget.cpp NativeOnInitialized)
  - Kiosk E-key confirm - AjinzzaInteractableKiosk::EnterKioskUIMode (covers
    RoomSettings/Wardrobe/FriendInvite) and AjinzzaStartMatchKiosk::Interact
    (no panel, so the sound is the only feedback there)
  - Role assignment reveal - AjinzzaGamePlayerController::Client_ReceiveRoleAssignment_Implementation
  - Round phase transitions - new AjinzzaGamePlayerController::HandlePhaseChanged,
    bound to AjinzzaGameGameState::OnPhaseChanged in BeginPlay/unbound in EndPlay
  - All 4 emotes - AjinzzaCharacter gained ThumbsUpSound/ThumbsDownSound/
    MiddleFingerSound/PointSound (EditAnywhere, defaulted via ConstructorHelpers
    same as the footstep/jump/land sounds), played in
    Multicast_PlayEmote_Implementation *before* the montage null-check so
    emotes are audible today even with zero animation content
  - Did NOT add: role-assignment/phase-transition/emote sounds were the only
    ones in §8 with an existing code hook to attach to - everything else left
    "예정" (question submit/reveal, vote confirm, ghost transition, final
    reveal fanfare, win/lose) is tied to Question Time/Voting/Interview/Ghost
    systems that don't exist in code yet, not just missing audio.
- Found and fixed a real UX gap while reading UjinzzaEmoteWheelWidget for the
  above: unlike every other widget in this project, its header explicitly
  said it "needs no BindWidget children" and left the actual visual entirely
  to a hand-authored WBP_EmoteWheel - which (same as every other WBP in this
  project) was never actually laid out, meaning holding E currently shows
  nothing at all on screen even though the hover-detection math underneath
  already works correctly. Gave it the same BuildWidgetTree() treatment as
  the other 7 widgets that got it: a plain text cross layout (Thumbs Up/Down/
  Middle Finger/Point at N/S/W/E), recolored gold when hovered via a new
  RefreshHighlight() called from NativeTick alongside the existing
  BP_OnHoveredEmoteChanged (left in place for a future hand-authored
  WBP_EmoteWheel to still hook). Closes docs/JINZZA_todo.xlsx row177
  ("라디얼 메뉴 UI 시안/구현") on the implementation half - the concept-art
  half is still a real open design/art task.
- Deliberately NOT attempted (would need a whole new subsystem, a new
  in-world zone blockout, new input-mapping assets, or a human decision -
  see docs/JINZZA_todo.xlsx for the full remaining list):
  - Week 7/8 systems: UQuestionTimeSubsystem, AVoteManager + vote UI,
    interview-target assignment + isolated voice channel, ghost-state
    transition + spectate HUD - none of these exist in any form yet; building
    them now without a scoped design pass first risks a lot of guessed,
    untested gameplay code landing at once.
  - Week 9 map blockout (6 zones in Lvl_Game, teleport triggers, isolation
    volume, stages) - has little value before the phase systems above exist
    to actually use those zones for something.
  - New noise props (Bell/AirHorn/Drum/Whistle/ConfettiCannon) - the design
    doc's original 8-prop list; these don't exist as classes yet at all
    (Boombox/Basketball/BasketballHoop/StunGun were built instead, per
    2026-09-08's own row15/16/186 - a real doc/implementation divergence the
    sheet flags as needing a decision, not something to silently resolve).
  - Emote hotkeys (1-4, row178) - needs new IA_Emote1-4 Input Actions and an
    IMC_Default mapping addition; the Enhanced Input mapping-array
    read/write limitation (see Sprint note above) applies here too, so this
    needs the same manual in-editor step rather than a blind tool-driven one.
  - 331 uncommitted changes / git push (row27/210) - explicitly did not
    commit anything without being asked, per this session's standing
    instructions; flagging that the sheet lists this as outstanding.
- [ ] NOT compiled or playtested this session (no build-trigger tool, as
  always). Touches jinzzaGameGameMode.cpp, jinzzaSettingsWidget.h/.cpp,
  jinzzaMatchSettings.h, jinzzaRoundPhaseSubsystem.cpp,
  jinzzaRoomSettingsWidget.h/.cpp, jinzzaInteractableKiosk.cpp,
  jinzzaStartMatchKiosk.cpp, jinzzaMainMenuWidget.cpp, jinzzaLobbyWidget.cpp,
  jinzzaGamePlayerController.h/.cpp, jinzzaCharacter.h/.cpp,
  jinzzaEmoteWheelWidget.h/.cpp - a big batch across many files, please Live
  Coding compile and check Saved/Logs/jinzza.log before relying on any of it.
  Suggested playtest order: Settings Controls tab (Shoot/SwapWeapon rows
  gone, Jump/Sprint still there) -> Room Settings kiosk (new Question Time
  Cycles row) -> hold E for the emote wheel (should show 4 labeled
  directions now, highlighting on hover) -> a 3-player round for role-reveal/
  phase-transition sounds.
- Changes are NOT committed.

=================================================================
DONE THIS SESSION (2026-09-08, continued, by Claude) - real local voice
modulation (Pitch/Robot/Cave Echo sliders + Cave/Helium/Robot templates),
and fixed real problems found in Lvl_test while turning it into a proper
walkable exhibition
=================================================================
- User asked for voice modulation with direct pitch control + a few simple
  settings + three named templates (cave/helium/robot voices), and separately
  flagged that Lvl_test "isn't properly set up" and asked for it to read like
  an exhibition characters can walk through to test each feature.
- Read the actual UE 5.8 engine source (Engine/Plugins/Runtime/Synthesis/...,
  Engine/Source/.../SynthComponent.h) directly rather than assuming API
  shapes, since this session had no prior confirmed knowledge of the
  Synthesis plugin's exact class/property names. Found: no dedicated
  "pitch shifter" source effect exists in this engine version - pitch is
  meant to go through the simpler UAudioComponent::SetPitchMultiplier
  (which is also just... the actual mechanism real chipmunk/helium and
  slowed-down/cave voice memes use, so no DSP plugin needed for that part
  at all); USynthComponent::SourceEffectChain is a public, EditAnywhere
  TObjectPtr<USoundEffectSourcePresetChain>, settable from plain C++.
- jinzza.Build.cs: added "Synthesis" (NEW dependency - the plugin was
  already enabled in jinzza.uproject, but linking its classes needs the
  module too, same two-step gotcha as AudioCapture/AudioMixer). Unlike that
  earlier addition, this one's Live-Coding-vs-full-rebuild fate is
  unverified - try Live Coding first, but be ready for a full close-editor
  rebuild if it fails to link.
- jinzzaVoiceTestWidget.h/.cpp: full rewrite. Real BuildWidgetTree() (it was
  UMG-authored/BindWidgetOptional and, since nothing in the project ever
  actually constructed a VoiceTestWidget anywhere, was 100% dead code before
  this session - not just unstyled, literally never instantiated). Three
  sliders (Pitch 0.5-2.0x, Robot 0-100%, Cave Echo 0-100%) apply live while
  dragging - Pitch restarts the capture component to pick up the change
  (same restart-on-change behavior the old None/High/Low version already
  had), Robot/Echo push updates through their preset objects' SetSettings()
  instead, no restart needed. Three template buttons (Cave/Helium/Robot) set
  all three sliders at once: Cave = 0.65x pitch + 60% echo, Helium = 1.7x
  pitch alone, Robot = 0.9x pitch + 85% ring-mod + 15% echo. Deliberately
  built as its own independent slider state, NOT reusing EJinzzaVoiceFilter
  (that enum is the round-disguise mechanism's - collapsing them would risk
  exactly the collision AjinzzaStunGunProp's class comment already warned
  against for a different feature).
- New AjinzzaVoiceTestKiosk (jinzzaVoiceTestKiosk.h/.cpp) - mirrors
  AjinzzaWardrobeKiosk exactly (cylinder mesh, floating label, opens the
  widget as a centered popup via EnterKioskUIMode/ExitKioskUIMode). Placed
  in Lvl_test's exhibition (see below) but NOT yet placed anywhere - it's a
  brand-new C++ class, can't be spawned via SceneTools until it exists in
  the compiled reflection system (same standing limitation as every prior
  new-class session).
- jinzzaMainMenuWidget.cpp: VoiceTestWidget is now actually constructed
  (bottom-right corner of RootOverlay, same nested-widget pattern as
  Settings/Customization) - previously declared in the header and referenced
  in comments but never built, so it silently did nothing.
- Lvl_test - real problems found and fixed, not just polish:
  - No DirectionalLight or SkyLight existed anywhere in the level - it would
    PIE almost pitch black. Added both, matching Lvl_Lobby's DirectionalLight
    rotation (Pitch -50, Yaw 20).
  - Extended the floor (a scaled cube) from a 3900x1000 corridor to
    5100x1000 to fit two more zones.
  - Added Zone 8 (Jump Pad, BP_JumpPad, at the new floor's far end) and Zone
    9 (Wardrobe kiosk, moved in from its originally-planned zone-5 slot).
  - Caught and fixed a real placement mistake mid-session: zone spacing in
    this level is a fixed ~460 units (zones 1-7 already occupy 0/460/920/
    1380/1840/2300/2760), and zone 5 (1840) is deliberately propless per
    Lvl_Test_README.txt - "Emote Wheel, no prop, tests AjinzzaCharacter
    itself via E-hold". First pass placed the new Jump Pad directly on top
    of that documented-but-empty slot without noticing; caught it by
    actually reading Lvl_Test_README.txt (which [[test-level-convention]]
    says to keep in sync) before writing the summary, and moved Jump Pad to
    a genuinely new zone 8 (3220) and Wardrobe to zone 9 (3680) instead.
  - Zone 10 (4140) is reserved in the README for the Voice Test kiosk once
    it can be placed post-compile - floor already extended to cover it.
  - Updated Lvl_Test_README.txt for all of the above, per its own stated
    convention of staying in sync with the level.
- [ ] NOT compiled - and this one specifically needs verifying whether Live
  Coding is enough or a full rebuild is required (see the Synthesis
  dependency note above). After compiling: place AjinzzaVoiceTestKiosk at
  (4140, 0, 80) in Lvl_test (ask Claude to do this via SceneTools once the
  class exists in the reflection system), then playtest the whole exhibition
  corridor end to end, and specifically confirm the mic capture + three
  sliders + three template buttons all produce an audible difference (this
  session could not playtest audio at all - no way to hear output from this
  session).
- Changes are NOT committed.

=================================================================
DONE THIS SESSION (2026-09-08, continued, by Claude) - fixed the build error
from the voice-modulation pass, and added section-divider walls to Lvl_test
=================================================================
- User ran the (Live Coding, not a full rebuild - so Synthesis apparently
  linked fine without needing one) build and hit a real C2664 in
  jinzzaVoiceTestWidget.cpp: the local AddSliderRow() helper's out-param was
  typed `UTextBlock*&`, but PitchValueText/RobotValueText/EchoValueText are
  `TObjectPtr<UTextBlock>` members - same TObjectPtr-vs-raw-pointer-reference
  mismatch already hit and fixed once before in jinzzaCustomizationWidget.cpp,
  just missed here. Fixed by retyping the parameter to
  `TObjectPtr<UTextBlock>&`.
- User asked for partition walls in Lvl_test, "a wall with only one side
  open using cubes in each section", to visually distinguish each zone.
  Added a low (220 tall, 30 thick) divider between every pair of adjacent
  zones (9 dividers for the 10 zones), each built from 2 SM_Cube segments
  (LevelPrototyping kit, corner-pivoted 100x100x100 - confirmed via
  get_actor_bounds before scripting the rest) covering 300 of the 600-unit
  span each, leaving the other 300 open so the corridor stays fully
  walkable - which side is open alternates per divider for a slight zigzag.
  Used the unreal-mcp ProgrammaticToolset (a sandboxed Python script that
  calls other tools) to place all 18 pieces in one round-trip instead of 18
  separate calls - verified two segments' bounds afterward to confirm the
  math landed exactly as intended before saving. All 18 live in a
  "SectionDividers" outliner folder. Documented in Lvl_Test_README.txt per
  [[test-level-convention]].
- [ ] Still not compiled/playtested by this session for the divider change
  (it's pure level-editor placement, no C++, so nothing to compile - but
  please PIE Lvl_test and walk the corridor to confirm the zigzag reads
  well and doesn't feel awkward; happy to adjust widths/heights/alternation
  pattern).
- Changes are NOT committed.

=================================================================
DONE THIS SESSION (2026-09-08, continued, by Claude) - build fix confirmed
working, replaced the zigzag dividers with proper U-shaped exhibit booths
=================================================================
- Good news buried in the user's report: the Live Coding build (not a full
  rebuild) got all the way to Module.jinzza.cpp and only failed on the
  TObjectPtr/UTextBlock*& type error - meaning the new "Synthesis" Build.cs
  dependency from the voice-modulation pass linked fine via Live Coding,
  no full rebuild was actually needed after all.
- User asked to "list each test actor as a single character" - asked a
  clarifying question rather than guessing between two very different
  builds (a single-letter sign vs. spawning NPC mannequins); the actual
  answer was neither - it meant keep the actors arranged in a single
  straight line. Found the one actor not on it: BP_BasketballHoop was
  offset to Y=300 (paired with the Basketball's Y=0) - recentered it to
  Y=0 like everything else.
- Replaced the previous zigzag section-divider walls (which partially
  blocked the main walkway between zones) with a proper U-shaped booth per
  zone instead, per explicit follow-up feedback that dividers weren't what
  was wanted. Removed all 18 old divider pieces, added 40 new ones: each
  zone's prop/kiosk now sits at the mouth of its own booth (back wall + 2
  side arms, alternating which side of the centerline it's built on) built
  from SM_Cube segments the same way as before. Critically, the booths sit
  entirely to one side (+Y or -Y) of the Y=0 centerline the actors are on -
  the centerline itself is never touched by a booth wall, so the corridor
  stays 100% walkable in a straight line the whole way, satisfying both
  asks (single line of actors + U-shaped wrapping) at once rather than
  trading one off against the other.
- Verified the corner-pivot math against actual get_actor_bounds calls on
  one +Y-side and one -Y-side booth before saving (a mistake in the -Y
  branch's back-wall offset was caught and fixed *before* running the
  batch, not after - see the two script revisions in this session's tool
  history) - both came back exactly as designed.
- Used the ProgrammaticToolset again (delete 18 + move 1 + create 40, all
  in one script) rather than ~60 individual tool round-trips.
- Updated Lvl_Test_README.txt's dividers section to describe the booths
  instead, per [[test-level-convention]].
- [ ] Not playtested by this session (pure level editing, nothing to
  compile) - please PIE Lvl_test and walk the row to confirm the booths
  read well and the alternating side doesn't feel too busy; easy to adjust
  booth depth/width/height or make the alternation consistent (always same
  side) if preferred.
- Changes are NOT committed.

=================================================================
DONE THIS SESSION (2026-09-08, continued, by Claude) - revised the exhibit
booths per follow-up feedback: all face the same way now, actors sit at the
booth's center (not the mouth), floor widened
=================================================================
- User feedback on the alternating booths: make them all face the same
  direction, and move each actor to the center of its U instead of the
  mouth/entrance; also asked to widen the corridor ("vertical space" - the
  Y axis in a top-down view - "too cramped", extend the floor).
- Removed the 40 alternating-side booth pieces and rebuilt 40 new ones, all
  opening toward -Y now (uniform direction) instead of alternating. Booths
  also grew (half-width 100->120, mouth gap 50->80, depth 300->400) for
  the requested "more room" feel. Moved every zone's prop/kiosk AND its
  sign from Y=0 (the old centerline) to Y=280 (the new booth's vertical
  center) - X and each actor's own Z were left untouched. Widened the
  floor's Y extent from -500..500 to -500..600 to comfortably contain the
  now-deeper booths with margin to spare.
- Also fixed the one actor that had drifted off the single-line arrangement
  from last time: BP_BasketballHoop was still at its pre-previous-session
  Y=300 in one intermediate step - confirmed it's now at Y=280 with
  everything else.
- Hit and fixed a real unreal-mcp gotcha while doing this (now recorded in
  memory): ActorTools.set_actor_transform's `xform` does NOT actually
  implement "unset fields = don't change" for existing actors, despite that
  being its documented behavior. A follow-up call that only included
  `rotation` (meaning to leave location alone) reset every affected sign's
  location to (0,0,0); a separate call that only included `location` earlier
  in the same session had already silently reset rotation to identity the
  same way (harmless there only because those actors' rotation was already
  0). Caught via get_actor_transform verification before saving - re-fixed
  by resending the FULL transform (location+rotation+scale together) on
  every affected sign, then re-verified all 8 signs + 9 actors before
  saving. Nothing was left broken, but always pass the complete transform
  on this call from now on - see [[unreal-mcp-gotchas]] item 9.
- Used the ProgrammaticToolset for the whole rebuild + reposition pass
  again (delete 40, extend floor, create 40, move 9 actors, move 8 signs,
  fix the rotation regression, in a handful of script round-trips instead
  of 60+ individual calls).
- Updated Lvl_Test_README.txt to describe the final design and its two
  prior iterations, per [[test-level-convention]].
- [ ] Not playtested by this session (pure level editing, nothing to
  compile) - please PIE Lvl_test and walk it; the exhibits now require a
  short step to the +Y side of the centerline to reach, confirm that reads
  well and the booths don't feel too deep/shallow.
- Changes are NOT committed.

=================================================================
DONE THIS SESSION (2026-09-08, continued, by Claude) - third revision of
Lvl_test's exhibit booths: doubled width (forced a full re-space of every
zone), everything now faces its booth's open side, added a real material
=================================================================
- User asked for 3 things: double the U-booths' left/right width, make
  signs and actors face the direction each booth opens, and improve visuals
  with simpler materials - explicitly scoped to Lvl_test.
- Doubling booth_half_width (120->240) meant the arms would overlap the
  neighboring zone's at the old 460-unit spacing (needed >540 clearance,
  had 460) - re-spaced every zone to 700 units apart instead, which
  cascaded into recomputing every prop/kiosk/sign position, the floor's
  full X and Y extent, and all 40 booth pieces from scratch.
- Facing: signs now yaw=-90 (face -Y, the booth's open mouth). Generic
  props (Bat/Boombox/Megaphone/Basketball/Hoop/StunGun/JumpPad) also
  yaw=-90. Kiosks (Wardrobe) are yaw=+90 instead - AjinzzaWardrobeKiosk's
  own Label sub-component has a fixed +180 relative yaw baked into its
  C++ constructor, so +90 on the actor nets the same effective -Y-facing
  label as everything else, not a literal copy of the -90 value.
- Materials: created Content/JINZZA/Level/MI_ExhibitBoothWall, a
  MaterialInstanceConstant off the LevelPrototyping kit's M_FlatCol
  (flat neutral gray, Roughness 0.75, Metallic 0) via MaterialInstanceTools,
  and applied it as an OverrideMaterials[0] on all 40 booth SM_Cube pieces -
  replaces whatever busier default material SM_Cube ships with. Verified
  the OverrideMaterials property format on one throwaway test cube first
  (plain asset path string in the array, not a {refPath:...} wrapper -
  same convention as the very first prop-sound session) before committing
  to all 40, and verified one real piece's material read back correctly
  after the real run too.
- Re-verified everything (floor bounds, one booth's geometry+material, an
  actor, a kiosk, a sign) via get_actor_bounds/get_actor_transform/
  get_properties before saving - all matched the intended math, including
  confirming zero overlap between adjacent zones' booth arms at the new
  700 spacing.
- Updated Lvl_Test_README.txt (spacing note, zone descriptions, the
  Exhibit-booths section) per [[test-level-convention]] - now documents
  all three design iterations for context.
- [ ] Not playtested by this session (pure level/asset editing, nothing to
  compile) - please PIE Lvl_test and walk it. Worth specifically checking:
  the wider booths read as roomier rather than cavernous, the flat gray
  material actually looks calmer than before, and every sign/kiosk label is
  legible from the walkway (the rotation math was verified by number, not
  by eye - this session cannot render/see the level).
- Changes are NOT committed.

=================================================================
DONE THIS SESSION (2026-09-08, continued, by Claude) - composite prop
meshes, filled out every remaining zone (10-15), Korean translation of
all Lvl_test signage, 5 named sections
=================================================================
- User asked for 4 things, all scoped to Lvl_test: (1) combine multiple
  Engine basic shapes into better approximate prop meshes instead of a
  single primitive each, (2) add every prop/function not yet in the test
  map - explicitly "don't leave out any props or functions", (3) divide
  the level into sections, (4) write all in-level text in Korean.
- Composite meshes: added child StaticMeshComponents via PrimitiveTools to
  5 Blueprints' CDOs (BP_Bat, BP_Boombox, BP_Megaphone, BP_BasketballHoop,
  BP_StunGun) - e.g. Bat got a thicker "Barrel" cylinder near the top,
  Boombox got two speaker cylinders + a handle cube, Megaphone got a
  handle cylinder, BasketballHoop got a backboard cube + a pole cylinder
  down to the ground, StunGun got a barrel cylinder + a grip cube. All 5
  Blueprints compiled successfully (had to use the .BP_Name.BP_Name object
  path form for compile_blueprint, not just .BP_Name). Basketball itself
  left as a plain sphere - already accurate. Saved.
- Missing zones filled in: confirmed via search_subclasses that
  AjinzzaRoomSettingsKiosk, AjinzzaFriendInviteKiosk, AjinzzaStartMatchKiosk,
  and the brand-new AjinzzaVoiceTestKiosk are all now compiled/spawnable
  (VoiceTest had been a reserved-but-unplaced slot in the prior session).
  Placed all 4 as new zones 10-13, plus two LevelPrototyping-kit
  Interactable examples (BP_DoorFrame, BP_WobbleTarget) as zones 14-15
  with their own signs since they don't self-label. Level now has all 15
  known testable features/props represented, none left out.
- Re-spacing: rather than re-laying out all 15 zones, only moved what
  actually needed to move - retired/rebuilt booths for zones 5-8 and added
  new ones for 10-15 (X = 2800..9800, still 700 apart), extended the floor
  to X: -700..10200, and repositioned the 3 props (Boombox-steal, StunGun,
  JumpPad) plus the Emote Wheel sign that shifted zone numbers. Caught and
  fixed a bug where the first pass moved those 3 props but left their
  TextRenderActor signs behind at the old positions - re-verified via
  get_actor_transform before saving.
- Sections: added 5 TextRenderActor header signs on the Y=0 corridor
  centerline (yaw=180, larger WorldSize=40) - 구역 A 소음 소품 (zones 1-3),
  B 이동 (4,6,7,8), C 감정표현 (5), D 키오스크 & 로비 기능 (9-13),
  E 레벨 프로토타이핑 (14-15).
- Korean text: rewrote every existing zone sign, the welcome sign, and the
  Wardrobe kiosk's Label to Korean with explicit zone numbers; new kiosks'
  Labels and the 2 new prop signs were authored in Korean from the start.
- Font risk (flagged, not silently assumed fine): the project's only
  Korean UFont, SacheonUju-Regular_Font, has FontCacheType=Runtime (the
  Slate/UMG pipeline), not the "Offline Cached" bitmap format
  TextRenderComponent has traditionally required. Assigned it to all 16
  TextRenderComponents and all 5 kiosk Label components anyway (nothing
  else exists to try), but this session cannot render the level and could
  NOT visually confirm the Korean glyphs actually display on these 3D
  world-space signs - documented as a caveat in Lvl_Test_README.txt.
  UMG/Slate menus elsewhere already use this font successfully; the risk
  is specific to TextRenderComponent.
- Verified with get_actor_transform/get_properties spot-checks (sign
  repositions, kiosk transform+label text, door snap-to-ground height,
  header sign text) before calling save_assets.
- Updated Lvl_Test_README.txt: full rewrite of the zone list (1-15), the
  5 sections, the composite-mesh note, and the font caveat above.
- [ ] Not playtested by this session (pure Blueprint/level editing, no C++
  changes, nothing needed compiling for this pass specifically). Please
  PIE Lvl_test and check: the new composite prop shapes actually read
  better than single primitives, all 15 zones/signs are reachable and not
  overlapping, and - most importantly - whether the Korean text on the 3D
  signs renders correctly or shows blank/tofu glyphs (see font risk above).
- Changes are NOT committed.

=================================================================
DONE THIS SESSION (2026-09-08, continued, by Claude) - root-caused the
Korean sign font risk to a confirmed engine limitation and built the fix's
C++ half
=================================================================
- User reported the Korean text isn't showing up in Lvl_test and asked
  whether noob-game's copy of the SacheonUju font could be used instead.
- Checked noob-game's font files directly: every one of them
  (SacheonUju-Regular_Font.uasset, SacheonUju-Regular.uasset,
  SacheonUjuOTF-Regular.uasset) is a ~130-byte Git LFS pointer, not real
  data - same dead end as [[fkey-icon-lfs-pointer]]. Not usable.
- Instead traced the actual root cause in the UE 5.8 engine source
  (Engine/Source/Runtime/Engine/Private/Components/TextRenderComponent.cpp,
  FTextRenderSceneProxy::CreateRenderThreadResources, ~line 729): when the
  assigned Font's FontCacheType is Runtime, the function returns
  immediately and builds zero mesh - "Runtime fonts can't currently be
  used here as they use the font cache from Slate application which can
  only be used on the game thread." This is a hard, confirmed engine
  limitation, not a maybe - TextRenderComponent categorically cannot
  render this project's only Korean font. See [[korean-font-textrender-risk]].
- Checked whether re-importing as an Offline Cached font was feasible: the
  original source file (SacheonUju-Regular.ttf/.otf) was imported from
  E:\Downloads\사천우주체\SacheonUju\ on a different machine - no E: drive
  exists here, and the embedded font bytes inside the existing FontFace
  .uasset aren't exposed through any reflected property this session can
  read, so reconstructing/re-importing it wasn't possible this session.
- Landed on the real fix: UTextRenderComponent can't render this font, but
  UMG/Slate already does (used successfully by every menu widget) - so
  world-space signs need to move from TextRenderComponent to a
  UWidgetComponent showing a small UMG widget instead.
- Confirmed via engine source (WidgetComponent.cpp's Plane geometry-mode
  vertex build, TangentZ=(1,0,0)) that a UWidgetComponent's readable face
  points along its own local +X axis - same facing math as everything
  else in Lvl_test, so the existing yaw=-90-faces-(-Y) convention carries
  over unchanged to the new sign actors.
- Built the C++ half: UjinzzaWorldSignWidget (UUserWidget with a
  BindWidgetOptional TextBlock named SignText + SetSignText, mirrors
  UjinzzaInteractionPromptWidget's pattern) and AjinzzaWorldSignActor (AActor
  wrapping a World-space UWidgetComponent, with editable SignText/
  SignWidgetClass/SignSize properties, pushing text into the widget from
  OnConstruction/PostEditChangeProperty/BeginPlay so it stays live in both
  editor and PIE).
- [ ] NOT YET COMPILED - these are brand-new files, needs a real rebuild
  (Live Coding won't pick up new classes), not just Live Coding.
- [ ] NOT YET WIRED INTO THE LEVEL - blocked on a manual step only the user
  can do: author WBP_WorldSign (parent = JinzzaWorldSignWidget) with one
  TextBlock named "SignText", font = SacheonUju-Regular_Font, centered,
  auto-wrap, then compile/save it. Once that exists, the next session
  should replace Lvl_test's 16 standalone TextRenderActor signs with
  AjinzzaWorldSignActor instances (spawn + set SignText/SignWidgetClass/
  transform per instance, same positions/rotations as today).
- [ ] Also flagged, not yet actioned: the 5 kiosk Label components
  (Wardrobe/RoomSettings/FriendInvite/StartMatch/VoiceTest) have the exact
  same TextRenderComponent-Runtime-font problem, baked into the shared
  AjinzzaInteractableKiosk-family C++ classes - meaning kiosk labels are
  also blank in Lvl_Lobby, not just Lvl_test. Same WidgetComponent fix
  would apply but touches 5 gameplay classes, not just test-level
  dressing - user hasn't confirmed whether to fold this in yet.
- Changes committed and pushed this round: the two new class pairs only
  (jinzzaWorldSignWidget.h/.cpp, jinzzaWorldSignActor.h/.cpp). No level or
  Blueprint asset changes this round.

=================================================================
NOTES
=================================================================
- Section 16 of the doc flags these as still-undecided design details to
  nail down before or during their respective build weeks:
  - Emote radial menu UI concept art
  - Noise-prop balance (radius/cooldown/judge-attention impact) - deferred
    to in-game playtesting rather than spec'd up front
  - 2-judge mode's vote/interview rules
  - "Candidate vote" mode for choosing who is the Real One - full flow
    (who votes when, how it stays hidden from the judge)
